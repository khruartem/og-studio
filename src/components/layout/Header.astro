---
import Logo from "../branding/Logo.astro";
import Menu from "../ui/Menu.astro";
import Button from "../ui/Button.astro";

import { navigation as items } from "../../data/navigation";
import Container from "./Container.astro";
import MobileMenu from "../ui/MobileMenu";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props as Props;
---

<header class:list={["py-2 fixed top-0 left-0 w-full md:py-3 2xl:py-5 ", className]}>
  <Container class="flex justify-between items-center">
    <Logo variant="dark" class="w-32 md:w-40" />
    <div
      class="flex flex-row-reverse gap-8 items-center lg:flex-row lg:gap-15"
    >
      <nav aria-label="Навигация">
        <Menu items={items} class="hidden lg:flex" />
        <MobileMenu items={items} class="flex flex-col gap-4 lg:hidden" />
      </nav>
      <Button shape="arrow" class="hidden md:inline-flex">Написать нам</Button>
    </div>
  </Container>
</header>
